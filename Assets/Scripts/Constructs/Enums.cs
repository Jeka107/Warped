using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Enums
{
    public enum ITEM_COLLECT_TAGS
    { 
        Collectable,
        ConsumeCollectable,
        AttackCollectable
    }

    public enum HIT_OBJECT_TAGS
    {
        Draggable,
        Useable,
        Wearable,
        Ground
    }

    public enum HIT_OBJECT_Layers
    {
        Draggable,
        ItemHold
    }
    public enum KEY_CODES
    {
        E,F,Q,R,Mouse0,
    }
}
